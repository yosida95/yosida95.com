machine:
    timezone: Asia/Tokyo
    python:
        version: 3.5.0
dependencies:
    override:
        - pip install 'tinkerer==1.5'
test:
    override:
        - tinker -b
deployment:
    tinkerer:
        branch: master
        commands:
            - deploy/production.sh
