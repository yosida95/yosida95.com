---
machine:
  python:
    version: 3.6.1
dependencies:
  override:
    - pip install 'tinkerer==1.6.0'
test:
  override:
    - tinker -b
deployment:
  tinkerer:
    branch: master
    commands:
      - deploy/production.sh
...
