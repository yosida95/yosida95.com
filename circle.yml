machine:
    timezone: Asia/Tokyo
    python:
        version: 3.5.0
dependencies:
    override:
        - pip install --upgrade 'tinkerer==1.5'
test:
    override:
        - tinker -b
deployment:
    tinkerer:
        branch: blog
        commands:
            - deploy/production.sh
